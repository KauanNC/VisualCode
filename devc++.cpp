#include <stdio.h>
#include <stdlib.h>
#include <locale.h>

int main()
{
    setlocale(LC_ALL, "portuguese");

   int i;

// minha resolução
 //for ( i = 0 ; i <= 15; i=i+1)
 //{
 //  printf ("%d\n",i);
 //}

//resolução do profdessor
 for ( i = 1 ; i <= 15; i++) {
 	if(i % 2 ! = 0) }
   printf ("%d\n",i);
 }
}
    return 0;
}
